import { useFeedbackLanguage } from "~/utils/use-feedback-language";

/** The feedback language Writing should request: the preference, or the interface language. */
export const useWritingFeedbackLanguage = useFeedbackLanguage;
